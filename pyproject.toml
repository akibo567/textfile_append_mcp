[project]
name = "textfile-append-mcp"
version = "0.1.0"
description = "Minimal MCP server to trim lines from the end of a file and append text."
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
